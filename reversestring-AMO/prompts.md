Persona:
Actúa como un desarrollador frontend experimentado con conocimientos de estructura de proyectos y buenas prácticas en HTML y JavaScript.

Context:
Quiero resolver un reto que consiste en crear una página web que invierta una cadena de texto. Se parte de una plantilla base (carpeta /template). La solución final debe estar en una carpeta nueva llamada `reversestring-AMO` y debe replicar la estructura de la plantilla base.

Desired Outcome:
Crea una carpeta llamada `reversestring-AMO` que incluya:
1. Un archivo `index.html` con un input de texto, un botón (solo visible si hay más de 3 caracteres) y un área donde se muestre la cadena invertida.
2. Un archivo `scripts.js` que contenga la lógica para:
   - Invertir la cadena.
   - Mostrar el resultado.
   - Hacer que el resultado se actualice en tiempo real sin necesidad del botón (nivel opcional).

Tone + Style:
Código claro, comentado, separado por responsabilidades (HTML y JS por separado), sin frameworks. Sigue buenas prácticas de accesibilidad y usabilidad web.

#Of Options:
No generes todo en un solo archivo. Crea los tres archivos (`index.html`, `scripts.js`, `prompts.md`) dentro de la carpeta `reversestring-AMO`.

Output Style:
Devuelve solo el contenido de cada archivo con su nombre como título (usando comentarios como: `// reversestring-AMO/index.html`), y asegúrate de que estén correctamente separados. No des explicaciones adicionales ni mezclas todo en un solo bloque.
