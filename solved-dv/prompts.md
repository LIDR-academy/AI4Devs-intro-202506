Prompt 1: 
You're a web developer

Your goal is to create a simple webpage that will reverse a string received as input

Example:
Input:"text"
Output:"txet"

Use the attached HTML and JS files as base. You mus re-use what is there and extend it.
Make it look like the attached image. As close as possible. If you're using CSS, embed it in the HTML, don't create an external css file. index.html
script.js

Validate instructions and ask me any clarifing questions you may have before proceeding


Prompt 2:
Only when the button is clicked
Copy only the reversed string. Not the source string

