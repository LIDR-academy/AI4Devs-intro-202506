# Prompts used to generate this solution

**Chatbot:** Claude Sonnet 4.6 (claude-sonnet-4-6) via Claude Code CLI

---

## Prompt 1

> Create a simple web interface with the following features:
> - An input field where the user can type text.
> - A button labeled "Reverse Text" that reverses the input text when clicked and displays the reversed text to the user.
> - Another button labeled "Copy" that copies the reversed text to the clipboard.
>
> Technical constraints:
> - Use vanilla JavaScript (no frameworks).
> - Follow best practices (clean code, separation of concerns).
> - Apply a modern and minimalist UI design using CSS.
>
> Implement the solution using:
> - `/solved-reverse-string-hamf/index.html`
> - `/solved-reverse-string-hamf/script.js`
> - `/solved-reverse-string-hamf/styles.css`
>
> Copy the template from the provided example image.

---

## Prompt 2

> We can improve the styles adding some gradient background and center the content — simple but looking good. Show the button for reverse when the input has more than 3 letters.

---

## Prompt 3

> Sorry, not words — letters.

*(Correction to Prompt 2: the threshold should be characters, not words.)*

---

## Prompt 4

> Nice. Now add a clear button, and remove the button for reverse doing it in real time — the conversion when we have more than 3 letters, could be showed in the bottom of the input.
