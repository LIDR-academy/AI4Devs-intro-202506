
#1 Reverse string prompt
Create a **web page with JavaScript logic that reverses the order of a string**.

Example: if I input AI4Devs, it should return sveD4IA.

Use  provided files index.html 

Prepare all the necessary unit testing to validate for the developer the solution 
The web should be prepared for haxagonal architecture to customize in the future more functionalities.
Before execute the task, think about any other aspect I should provide you in the prompt

#2 Reverse string prompt

Create a **web page with JavaScript logic that reverses the order of a string**.

Example: if I input AI4Devs, it should return sveD4IA.

Use  provided files index.html 

Prepare all the necessary unit testing to validate for the developer the solution 
The web should be prepared for haxagonal architecture to customize in the future more functionalities.
Use react framework
Test environment should be Node.js CLI environment
For the hexagonal architecture, will future functionalities include:
- API integration

- Multiple transformations (e.g. uppercase, encoding)

- Internationalization support

Deployment Intent: in thos first approach the this will run purely on front end

UI Requirements: just a minimal form or something styled and user-friendly


#3
modifica la aplicacion para que:
El botón debe aparecer cuando haya texto suficiente, más de 3 letras
Que te de la cadena en tiempo real, que no dependa del botón.