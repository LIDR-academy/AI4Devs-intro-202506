Tengo una página creada como index.html que tiene un archivo script.js enlazado y necesito crear con html , js, y css lo siguiente:

1. que haya un título con un h1 que diga "Reverse String".

2. Debajo del título, incluir un input que capture el string que se va a revertir con su respectivo placeholder.

3. Que debajo del input haya un botón color azul que contenga el texto "reverse".

4. Al darle clic al botón, ejecute una función que permita invertir todos los caracteres del string capturado en input. Ejemplo: si introduzco AI4Devs devuelve sveD4AI.

4. agrega una validación al botón para que este solo sea visible si el input contiene más de 3 caracteres.

5. el String reversado debe poder visualizarse debajo del botón.
