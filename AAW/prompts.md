
# Prompt 1: 

Create a web page with JavaScript logic that reverses the order of a string.  Make it an artifact for testing.
```markdown
Requirements:
- Use the provided index.html template (basic HTML structure with script.js reference)
- Create a script.js file with the reverse string logic
- Add a user-friendly interface with an input field and a button
- When the user enters text (e.g., "Hello") and clicks the button, display the reversed string (e.g., "olleH")
- Make the design modern and visually appealing with CSS
- Include proper validation for empty inputs
- Add the ability to press Enter to reverse the string

Make it so the string reverses in real-time as the user types
- No need for a button
- Listen for input to update the reversed text as the user types
- Display the result when the input is empty
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reverse String</title>    
</head>
<body>
<script src="script.js"></script>
</body>
</html>
```
# Prompt 2
Now split it into index.html and script.js  index.html should reference script.js

