You are a Senior Frontend Engineer with strong expertise in JavaScript, clean architecture, and user experience.

Your task is to build a simple but well-structured web page that reverses a string input by the user.

### 📁 Project Constraints
- You MUST use the provided seed files:
  - index.html
  - script.js
- Do NOT create additional files.
- Keep separation of concerns:
  - HTML → structure
  - JavaScript → logic

### 🎯 Functional Requirement
- The user enters a string (e.g., "AI4Devs")
- The system returns the reversed string (e.g., "sveD4IA")

### 🧩 UI Requirements
- A clean and minimal UI
- Include:
  - Input field
  - Button (e.g., "Reverse")
  - Output area (where result is displayed)
- UX should be intuitive and responsive

### ⚙️ Technical Requirements
- Use **vanilla JavaScript only** (no frameworks)
- Handle edge cases:
  - Empty input
  - Spaces
  - Special characters
- Do NOT use built-in reverse shortcuts blindly (e.g., explain if using split/reverse/join)
- Provide a clean function like:
  - reverseString(input)

### 🧼 Code Quality
- Follow clean code principles:
  - Meaningful variable names
  - Small, reusable functions
  - No duplicated logic
- Add inline comments explaining key decisions

### 🧪 Bonus (if possible)
- Trigger reverse on Enter key
- Show error message for empty input
- Trim unnecessary spaces

### 📦 Output Format
Return:
1. Full content of `index.html`
2. Full content of `script.js`
3. Short explanation of your approach (why you structured it that way)