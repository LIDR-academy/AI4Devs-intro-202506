CHATGPT
---
#1
Create a web page with javascript logic that reverses the order of a text string. The interface should look like in the image.

Example: if I enter AI4Devs it returns sveD4IA.

Do this by creating the index.html file and create the logic in a script.js file.

#2 
In this image is the directory structure. Please indicate in which directory I should place the "styles.css" file.

#3
Let me know how I can perform the local test of the string reversal

#4
Make the adjustments so that the "Reverse" button appears when there is enough text, more than 3 letters.

#5
where should I add the event listener inside the index.html file?

#6
Where and how I should apply the styling tip

#7
Adjust it so the 'Reverse' button disappears and the output updates in real time instead.

#8
Originally, I had a p#result section in the CSS file. Should I replace that section with this one?
"#result {
  min-height: 1.5em;
  font-size: 1.4em;
  margin-top: 1em;
}"